import Link from 'next/link'
import { MapPin, Clock3, Heart } from 'lucide-react'
import { AppHeader } from '@/components/navigation/app-header'
import { MapboxMap } from '@/components/map/mapbox-map'
import { requireProfile } from '@/lib/auth/session'
export const dynamic = 'force-dynamic'
export default async function PassengerPage() { const profile = await requireProfile(); if (profile.role === 'DRIVER') return <main><AppHeader profile={profile} /><section className="mx-auto max-w-xl p-8"><h1 className="font-serif text-3xl">Esta cuenta es de conductor.</h1><Link className="mt-5 inline-block text-primary" href="/driver">Ir al panel de conductor</Link></section></main>; return <main><AppHeader profile={profile} /><section className="mx-auto flex max-w-6xl flex-col gap-5 p-4 sm:p-6 lg:p-10"><div className="flex flex-col gap-2"><p className="text-xs font-bold uppercase tracking-[.18em] text-primary">Panel de pasajero · Formosa</p><h1 className="font-serif text-4xl text-balance sm:text-5xl">¿A dónde vamos?</h1><p className="max-w-xl leading-relaxed text-muted-foreground">Tu mapa, tus conductores cercanos y todo lo necesario para pedir un viaje.</p></div><div className="grid gap-5 lg:grid-cols-[minmax(0,1.3fr)_minmax(16rem,.7fr)]"><div className="min-h-[25rem] overflow-hidden rounded-3xl border border-border bg-secondary shadow-sm lg:min-h-[32rem]"><MapboxMap /></div><div className="flex flex-col gap-4"><Card icon={MapPin} title="Solicitar viaje" href="/passenger/ride" /><Card icon={Clock3} title="Tus viajes" href="/passenger/history" /><Card icon={Heart} title="Favoritos" href="/passenger/favorites" /></div></div></section></main> }
function Card({ icon: Icon, title, href }: { icon: typeof MapPin; title: string; href: string }) { return <Link href={href} className="rounded-2xl border border-border bg-card p-5 transition hover:border-primary"><Icon className="size-5 text-primary" /><p className="mt-8 font-semibold">{title}</p></Link> }
