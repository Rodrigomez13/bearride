import Link from 'next/link'
import { MapPin, Clock3, Heart } from 'lucide-react'
import { AppHeader } from '@/components/navigation/app-header'
import { MapboxMap } from '@/components/map/mapbox-map'
import { requireProfile } from '@/lib/auth/session'
export const dynamic = 'force-dynamic'
export default async function PassengerPage() { const profile = await requireProfile(); if (profile.role === 'DRIVER') return <main><AppHeader profile={profile} /><section className="mx-auto max-w-xl p-8"><h1 className="font-serif text-3xl">Esta cuenta es de conductor.</h1><Link className="mt-5 inline-block text-primary" href="/driver">Ir al panel de conductor</Link></section></main>; return <main><AppHeader profile={profile} /><section className="mx-auto flex max-w-7xl flex-col gap-7 p-4 sm:p-6 lg:p-10"><div className="flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p className="text-xs font-bold uppercase tracking-[.2em] text-accent">Tu movilidad, más humana</p><h1 className="mt-2 max-w-2xl font-serif text-4xl leading-[.98] tracking-tight text-balance sm:text-6xl">Movete por la ciudad con confianza.</h1><p className="mt-4 max-w-xl leading-relaxed text-muted-foreground">Conductores verificados, precios claros y una experiencia pensada para Formosa.</p></div><Link href="/passenger/ride" className="inline-flex min-h-12 items-center justify-center rounded-full bg-primary px-5 font-bold text-primary-foreground transition hover:opacity-90">Pedir un viaje <span aria-hidden="true" className="ml-2">→</span></Link></div><div className="grid gap-5 lg:grid-cols-[minmax(0,1.45fr)_minmax(18rem,.55fr)]"><div className="min-h-[25rem] overflow-hidden rounded-[2rem] border border-border bg-secondary shadow-lg lg:min-h-[34rem]"><MapboxMap /></div><div className="flex flex-col gap-4"><div className="rounded-[2rem] bg-accent p-6 text-primary-foreground"><p className="text-xs font-bold uppercase tracking-[.16em] opacity-70">Disponibilidad ahora</p><p className="mt-3 font-serif text-4xl">4 autos</p><p className="mt-2 text-sm leading-relaxed opacity-80">Conductores activos cerca de tu ubicación.</p></div><Card icon={MapPin} title="Solicitar viaje" href="/passenger/ride" /><Card icon={Clock3} title="Tus viajes" href="/passenger/history" /><Card icon={Heart} title="Favoritos" href="/passenger/favorites" /></div></div></section></main> }
function Card({ icon: Icon, title, href }: { icon: typeof MapPin; title: string; href: string }) { return <Link href={href} className="rounded-2xl border border-border bg-card p-5 transition hover:border-primary"><Icon className="size-5 text-primary" /><p className="mt-8 font-semibold">{title}</p></Link> }
