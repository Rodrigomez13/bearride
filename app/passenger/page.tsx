import Link from 'next/link'
import { MapPin, Clock3, Heart } from 'lucide-react'
import { AppHeader } from '@/components/navigation/app-header'
import { requireProfile } from '@/lib/auth/session'
export const dynamic = 'force-dynamic'
export default async function PassengerPage() { const profile = await requireProfile(); if (profile.role === 'DRIVER') return <main><AppHeader profile={profile} /><section className="mx-auto max-w-xl p-8"><h1 className="font-serif text-3xl">Esta cuenta es de conductor.</h1><Link className="mt-5 inline-block text-primary" href="/driver">Ir al panel de conductor</Link></section></main>; return <main><AppHeader profile={profile} /><section className="mx-auto max-w-4xl p-5 lg:p-10"><p className="text-xs font-bold uppercase tracking-[.18em] text-primary">Panel de pasajero</p><h1 className="mt-2 font-serif text-4xl">¿A dónde vamos?</h1><p className="mt-3 max-w-xl text-muted-foreground">Buscá tu destino, obtené una cotización calculada en servidor y confirmá cuando haya cobertura disponible.</p><div className="mt-8 grid gap-4 sm:grid-cols-3"><Card icon={MapPin} title="Solicitar viaje" href="/passenger/ride" /><Card icon={Clock3} title="Tus viajes" href="/passenger/history" /><Card icon={Heart} title="Favoritos" href="/passenger/favorites" /></div></section></main> }
function Card({ icon: Icon, title, href }: { icon: typeof MapPin; title: string; href: string }) { return <Link href={href} className="rounded-2xl border border-border bg-card p-5 transition hover:border-primary"><Icon className="size-5 text-primary" /><p className="mt-8 font-semibold">{title}</p></Link> }
